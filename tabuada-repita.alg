algoritmo "tabuada"
var
   i, n:inteiro
inicio
      i:= 1           //sempre precisa registrar o valor da variavel auxiliar
      escreval("escreva um valor: ")
      leia(n)

      repita
            escreval(i, "x", n,"=",i*n )
            i:= i + 1

      ate (i > 10 )
fimalgoritmo

	

algoritmo "repeticaoEmPar"
var
   contador: inteiro
inicio
      contador := 10

      enquanto (contador >= 0) faca

               Se (contador % 2 = 0) entao
                  escrevaL(contador)

               fimSe
               contador:= contador - 1
      fimEnquanto
fimalgoritmo
